extraction:
  cpp:
    prepare:    # Customizable step used by all languages.
      packages:
        - flex
        - python3-psutil
        - unifdef
        - llvm-9-dev
    configure:
      command: cmake -DCMAKE_BUILD_TYPE=DEBUG .
