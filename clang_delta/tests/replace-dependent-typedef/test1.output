typedef long xx_t;
typedef long xx;

template <class T> struct S { typedef T type; };
struct A { };
struct B { typedef S<A>::type type; };

template <typename T>
struct C {
  typedef int Inner;
  typedef C::Inner Bar;
};
